<?xml version="1.0" encoding="UTF-8"?>
<threadpool4j>
	<!-- 至少要有一个线程池default -->
	<pool name="default">
		<corePoolSize>3</corePoolSize>
		<maxPoolSize>10</maxPoolSize>
		<!-- 线程空闲存话的时间。单位：秒 -->
		<keepAliveTime>10</keepAliveTime>
		<workQueueSize>10000</workQueueSize>
	</pool>

    <!-- 线程池状态收集汇总配置
    switch: on-开; off-关
    interval: 单位(秒)
     -->
    <threadpoolstate switch="off" interval="60"></threadpoolstate>

    <!-- 线程状态收集汇总配置
    switch: on-开; off-关
    interval: 单位(秒)
     -->
    <threadstate switch="off" interval="60"></threadstate>
</threadpool4j>
